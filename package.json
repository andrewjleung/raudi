{
  "name": "raudi",
  "version": "0.0.0",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "server": "NODE_ENV=undockerizedDev CLIENT_PORT=8080 SERVER_PORT=3000 npm --prefix ./packages/server run start",
    "client": "NODE_ENV=undockerizedDev VITE_CLIENT_PORT=8080 VITE_SERVER_PORT=3000 npm --prefix ./packages/client run dev",
    "up": "docker compose up --build",
    "down": "docker compose down",
    "prune": "docker image prune -a"
  }
}
